import fs from 'fs';import path from 'path';import zlib from 'zlib';
let cache=null;
function load(){if(cache)return cache;const p=path.join(process.cwd(),'data','chapters.json.gz.b64');const b64=fs.readFileSync(p,'utf8').trim();const raw=zlib.gunzipSync(Buffer.from(b64,'base64')).toString('utf8');const docs=JSON.parse(raw);cache=docs.map(d=>{const ps=Array.isArray(d.paragraphs)?d.paragraphs:[];const re=new RegExp(`^פרק\\s*${d.number}[:：]`);let i=ps.findIndex(x=>re.test(String(x)));if(i<0)i=0;return{number:d.number,sourceFile:d.sourceFile,title:ps[i]||`פרק ${d.number}`,subtitle:ps[i+1]||'',paragraphs:ps,paragraphCount:ps.length,characterCount:ps.reduce((n,x)=>n+String(x).length,0)}});return cache}
export default function handler(req,res){try{const chapters=load();if(req.method!=='GET'){res.setHeader('Allow','GET');return res.status(405).json({ok:false,error:'method not allowed'})}const n=Number(req.query?.number);if(Number.isInteger(n)){const chapter=chapters.find(x=>x.number===n);return chapter?res.json({ok:true,chapter}):res.status(404).json({ok:false,error:'chapter not found'})}return res.json({ok:true,chapters,total:chapters.length,paragraphs:chapters.reduce((n,x)=>n+x.paragraphCount,0),characters:chapters.reduce((n,x)=>n+x.characterCount,0),sourceMode:'lossless-docx-text'})}catch(e){console.error(e);return res.status(500).json({ok:false,error:'chapter corpus unavailable'})}}
