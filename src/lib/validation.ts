export type ValidationErrors=Record<string,string>;export function validateKnowledge(input:{title?:string;content?:string;source?:string}):ValidationErrors{const e:ValidationErrors={};if(!input.title?.trim())e.title='נדרשת כותרת';if((input.title||'').length>180)e.title='הכותרת ארוכה מדי';if((input.content||'').length>100000)e.content='התוכן גדול מדי';if(input.source?.trim()){try{new URL(input.source)}catch{e.source='המקור חייב להיות קישור תקין'}}return e}
